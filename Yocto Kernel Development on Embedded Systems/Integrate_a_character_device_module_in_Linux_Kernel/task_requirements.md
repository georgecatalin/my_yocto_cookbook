# Objective
Demonstrate the practical workflow to build and insert a character device driver/module into the Linux Kernel using Yocto Project and BeagleBone Black

* for the character device driver, this example is going to make use of https://github.com/Munawar-git/tab-module